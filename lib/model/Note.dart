class Note {
  String title;
  DateTime lastEdited;
  String content;
  String courseCode;
  Note({
    required this.title,
    required this.lastEdited,
    required this.content,
    required this.courseCode,
  });
}
